def test_deliberately_failing_probe() -> None:
    """Temporary: proves main's ruleset actually blocks a red pull request.
    Removed in the next commit on this branch."""
    assert False, "ruleset probe"
